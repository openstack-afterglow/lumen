## Why

Afterglow users report completed conversations retaining default titles, missing Perplexity source metadata, and unavailable context capacity. Diagnose the actual durable title and installed LiteLLM boundaries rather than assuming previous fixture coverage proves provider compatibility.

## What Changes

- Repair confirmed title enqueue/claim/provider/persistence failures and safe recovery while preserving manual title revision fences.
- Inspect Perplexity Sonar/Agent responses through installed LiteLLM and retain actual source metadata in the canonical citation contract.
- Resolve exact canonical context metadata and distinguish unavailable limits/counting/invalid budgets without guessing capacity.
- Add meaningful regressions and exercise real DB/worker paths with synthetic provider HTTP responses.

## Capabilities

### New Capabilities

- None.

### Modified Capabilities

- Durable title lifecycle, citation normalization and context status projection.

## Impact

Lumen owns provider execution, metadata and persistence. Afterglow owns presentation in its matching rapid change. Preserve tenant ownership, manual edits, immutable accepted-run snapshots, price admission, safe URL handling and no replay of ambiguous paid requests. No production mutations or deployment.
